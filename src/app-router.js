/**
 * router-loader处理约定
 */
import React from "react";
import { HashRouter as Router, Route, Switch } from "react-router-dom";
import Rmd from "@/components/Rmd";
// imports
export default () => {
  return (
    <Router>
      <Switch>
        {/* routers */}
      </Switch>
    </Router>
  );
};
